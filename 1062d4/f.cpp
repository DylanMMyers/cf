#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\f.exe
// g++ f.cpp -o f

// redefine the tree as some directed edges
// sweep from all of the "left" edges and define how many nodes have a subtree of size k on the "left"
// do the same on the right
// iter over each node and determine how many nodes have these valid subtrees on either side

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll tt = 1;
    cin >> tt;
    while (tt--) {
        
    }
}