#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\digit_dp.exe
// g++ digit_dp.cpp -o digit_dp

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll a, b; cin >> a >> b;
    vector<int> dp()
}