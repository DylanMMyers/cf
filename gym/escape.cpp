#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\escape.exe

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int vp, vd, t, f, c;
    cin >> vp >> vd >> t >> f >> c;
    // how many times do we need to loop the dragon?
    
}