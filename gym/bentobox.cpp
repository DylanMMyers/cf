#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\bentobox.exe

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << 15 - a - b - c - d;
}