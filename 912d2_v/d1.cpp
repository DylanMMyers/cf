#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\d1.exe

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n, q; cin >> n >> q;
    vector<int> v(n); for (int i = 0; i < n; i++) cin >> v[i];
    for (int z = 0; z < q; z++) {
        int k; cin >> k;
    }
}