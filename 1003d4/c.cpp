#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\c1.exe

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll tt;
    cin >> tt;
    while (tt--) {
        int n, m;
        cin >> n >> m;
        
    }
}