#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\c.exe
// g++ c.cpp -o c

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll tt = 1;
    cin >> tt;
    while (tt--) {
        ll n, h, k; cin >> n >> h >> k;
        vector<ll> v(n);
        for (int i = 0; i < n; i++) cin >> v[i];
        
    }
}