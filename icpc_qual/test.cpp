#include <bits/stdc++.h>
#define ll long long
// Get-Content t.txt | ./file.exe
// g++ file.cpp -o file
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    double x = 4.5;
    int y = 2;
    cout << (x - y) * (x - y);
}