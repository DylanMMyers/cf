#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> r1(n);
    vector<int> r2(n);

    for (int i = 0; i < n; i++) {
        cin >> r1[i];
    }
    for (int i = 0; i < n; i++) {
        cin >> r2[i];
    }

    vector<bool> 

    return 0;
}