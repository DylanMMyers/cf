#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\a.exe
 
void solve() {
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++) {
        cin >> v[i];
    }
    int first;
    for (int i = 0; i < n; i++) {
        if (v[i] == 1) {
            first = i;
            break;
        }
    }
    vector<int> ps(n + 1, 0);
    for (int i = 1; i <= n; i++) {
        ps[i] = ps[i - 1];
        if (v[i] == 1) ps[i]++;
    }
    for (int i = first + 1; i < n; i++) {
        if (v[i] == 0) {

        }
    }
    

}
 
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll tt;
    cin >> tt;
    while (tt--) {
        solve();
    }
}