#include <bits/stdc++.h>
#define ll long long
using namespace std;
// Get-Content t.txt | .\e.exe

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll tt = 1;
    //cin >> tt;
    while (tt--) {
        string s;
        cin >> s;
        string s2 = s;
        reverse(s2.begin(), s2.end());
        for (int i = 0; i < s.length() - 1; i++) {
            if (s[i] )
        }
    }
}